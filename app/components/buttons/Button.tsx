import React from "react";
import cn from "class-variance-authority";

type ButtonProps = {
  label?: string;
  className?: string;
  onClick?: () => void;
};

export const Button = () => {
  return;

  <div>Button</div>;
};
